// Package config loads config.yaml: default download path, machine ID,
// max payload size, authorized_clients path, and the node's network role
// (hub or peer, Fase 1) plus its hub connection target when acting as a
// peer.
package config
